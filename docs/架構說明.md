# MemoriaCore — 專案架構說明

> 最後更新：2026-03-28

---

## 目錄

1. [系統概覽](#系統概覽)
2. [目錄結構](#目錄結構)
3. [請求流程](#請求流程)
4. [核心模組說明](#核心模組說明)
5. [API 路由總覽](#api-路由總覽)
6. [前端介面](#前端介面)
7. [資料儲存](#資料儲存)
8. [LLM 路由系統](#llm-路由系統)
9. [服務端口](#服務端口)

---

## 系統概覽

MemoriaCore 是一套 AI 情境記憶引擎，以 FastAPI 作為後端核心，結合 BGE-M3 ONNX 嵌入模型實現雙軌向量檢索（稠密 + 稀疏），並搭配人格演進系統讓 AI 能持續自我觀察與反思。

支援多種前端接入：
- **Dashboard HTML** — 主要管理介面（`/static/dashboard.html`）
- **Streamlit UI** — 輔助管理後台（Port 8501）
- **Telegram Bot** — 聊天機器人接入
- **Unity WebSocket** — 遊戲客戶端接入

---

## 目錄結構

```
LLMTest_New/
├── api/                        # FastAPI 後端
│   ├── main.py                 # 應用程式組裝、lifespan 管理
│   ├── dependencies.py         # 全域 Singleton 初始化與注入
│   ├── session_manager.py      # 跨介面 Session 狀態管理
│   ├── telegram_bot.py         # Telegram Bot 整合
│   ├── models/
│   │   ├── requests.py         # Pydantic 請求 DTO
│   │   └── responses.py        # Pydantic 回應 DTO
│   └── routers/
│       ├── health.py           # GET /health
│       ├── chat_ws.py          # POST /chat/sync、/chat/stream-sync、WS /chat/stream
│       ├── session.py          # Session CRUD 與歷史記錄
│       ├── memory.py           # 記憶區塊 CRUD、搜尋、圖譜
│       ├── profile.py          # 使用者畫像管理
│       ├── system.py           # 系統設定、人格、Prompt、工具觸發
│       ├── prompts.py          # Prompt 範本管理（即時編輯）
│       ├── logs.py             # 系統日誌查詢
│       └── character.py        # 角色設定生成
│
├── core/                       # 核心引擎模組
│   ├── chat_orchestrator.py    # 對話流程主控（工具呼叫、SSE 串流）
│   ├── llm_gateway.py          # LLM 路由器（9 種任務類型）
│   ├── core_memory.py          # 記憶系統（向量搜尋、WAL SQLite）
│   ├── memory_analyzer.py      # 話題偏移偵測、記憶管線觸發
│   ├── personality_engine.py   # AI 自我觀察與人格反思
│   ├── preference_aggregator.py# 使用者偏好聚合
│   ├── prompt_manager.py       # Prompt 範本統一管理
│   ├── storage_manager.py      # SQLite WAL 非同步鎖定層
│   ├── conversation_session.py # 對話 Session 資料結構
│   ├── character_engine.py     # 角色設定擴充引擎
│   ├── background_gatherer.py  # 背景話題搜集循環
│   └── system_logger.py        # 統一系統日誌記錄器
│
├── tools/                      # LLM 工具實作（Tool Use）
│   ├── tavily.py               # Tavily 網路搜尋工具
│   ├── weather.py              # 即時天氣查詢工具
│   └── weather_cache.py        # 天氣資料快取管理
│   └── synthetic.py            # 合成測試資料生成
│
├── ui/                         # Streamlit UI 頁面
│   ├── chat.py                 # 對話介面
│   ├── settings.py             # 系統設定頁
│   ├── routing.py              # LLM 路由設定頁
│   ├── prompts.py              # Prompt 管理頁
│   ├── history.py              # 對話歷史頁
│   ├── db_manager.py           # 記憶資料庫管理頁
│   ├── log_viewer.py           # 日誌查看頁
│   └── character.py            # 角色設定頁
│
├── static/                     # 靜態 HTML 介面（由 FastAPI 伺服）
│   ├── dashboard.html          # 主控台（聊天 + Log/DB + 路由 + Prompt）
│   ├── log_viewer.html         # 系統日誌檢視器
│   └── db_viewer.html          # 記憶資料庫檢視器
│
├── tests/                      # Pytest 測試套件
│   ├── conftest.py
│   ├── test_memory_recall.py
│   ├── test_pipeline_e2e.py
│   ├── test_topic_shift.py
│   ├── test_core_insights.py
│   ├── test_user_profile.py
│   ├── test_preference_aggregation.py
│   ├── test_cosine_similarity.py
│   ├── test_config.py
│   └── ollama_sim.py           # Ollama 模擬器（測試用）
│
├── docs/                       # 文件
│   ├── 架構說明.md              # 本文件
│   ├── API_使用說明書.md        # REST / WebSocket API 參考
│   └── Unity_CSharp_使用說明書.md # Unity 客戶端整合指南
│
├── UnityClient/                # Unity C# 客戶端範例
│   └── Assets/Scripts/
│       └── Network/
│           └── NetworkConfig.cs
│
├── StreamingAssets/
│   └── Models/
│       └── model_quantized.onnx  # BGE-M3 int8 嵌入模型（需手動下載）
│
├── app.py                      # Streamlit 入口
├── start.bat                   # 一鍵啟動（FastAPI + Streamlit）
├── setup.bat                   # 環境安裝腳本
├── build_server.bat            # PyInstaller 打包腳本
├── prompts_default.json        # 預設 Prompt 範本庫
├── user_prefs.json             # 執行期設定（API Keys、路由、閾值）
├── ai_personality.md           # AI 人格演進檔案（由 PersonalityEngine 讀寫）
└── system_prompt.txt           # AI 系統提示詞範本
```

---

## 請求流程

```
Client
  │
  ├─ HTML Dashboard / Streamlit UI
  ├─ Telegram Bot
  └─ Unity WebSocket
          │
          ▼
  FastAPI  api/main.py  (Port 8088)
          │
          ├─ POST /api/v1/chat/stream-sync  (SSE 串流)
          │         │
          │         ▼
          │   ChatOrchestrator  (core/chat_orchestrator.py)
          │         │
          │         ├─ LLMRouter → /chat 任務  (core/llm_gateway.py)
          │         │       └─ 工具呼叫 (Tavily / Weather)
          │         │
          │         ├─ MemorySystem  (core/core_memory.py)
          │         │       ├─ 稠密向量檢索 (BGE-M3 ONNX)
          │         │       └─ 稀疏關鍵字檢索
          │         │
          │         ├─ MemoryAnalyzer  (core/memory_analyzer.py)
          │         │       ├─ 話題偏移偵測
          │         │       └─ 記憶管線觸發（切分 / 摘要 / 圖譜）
          │         │
          │         ├─ PersonalityEngine  (core/personality_engine.py)
          │         │       ├─ ai_observe：提取自我陳述
          │         │       └─ ai_reflect：反思更新人格檔案
          │         │
          │         ├─ PreferenceAggregator  (core/preference_aggregator.py)
          │         │
          │         └─ StorageManager  (core/storage_manager.py)
          │                 └─ SQLite WAL（memory_db / conversation.db）
          │
          └─ 其他路由 (memory, profile, system, prompts, logs...)
```

---

## 核心模組說明

### `core/chat_orchestrator.py`
對話主控器。負責協調整個對話回應流程：接收使用者訊息、組裝動態 Prompt、觸發 LLM 呼叫、處理多輪工具呼叫（Tool Use）、最終以 SSE 串流回應。

### `core/llm_gateway.py`
LLM 路由器。支援 9 種任務類型，每種可獨立設定 Provider 與 Model：
`chat`、`pipeline`、`expand`、`compress`、`distill`、`ep_fuse`、`profile`、`ai_observe`、`ai_reflect`、`background_gather`、`character_gen`、`router`

支援的 Provider：**Ollama（本地）**、**llama.cpp（本地）**、**OpenAI（雲端）**、**OpenRouter（雲端）**

### `core/core_memory.py`
記憶系統。以 SQLite WAL 模式儲存記憶區塊，實現混合向量搜尋（稠密 + 稀疏 + 時間衰減 + 重要性加權）。每個 LLM 模型擁有獨立的資料庫檔案（`memory_db_*.db`）。

### `core/memory_analyzer.py`
記憶分析器。在對話過程中偵測話題偏移，在背景觸發記憶管線（長文切分、摘要生成、圖譜縫合）。

### `core/personality_engine.py`
人格引擎。從對話中提取 AI 的自我陳述（`ai_observe`），達到閾值後觸發反思（`ai_reflect`），並將更新結果寫入 `ai_personality.md`。

### `core/prompt_manager.py`
Prompt 統一管理器。從 `prompts_default.json` 讀取預設範本，支援使用者透過 Dashboard 即時覆寫，並在執行期將佔位符替換為實際內容。

### `core/storage_manager.py`
儲存層。封裝所有 SQLite 操作，使用 WAL 模式與非同步鎖定機制確保並發安全。

### `api/dependencies.py`
FastAPI 依賴注入中心。所有核心模組以 Singleton 形式初始化，透過 `Depends()` 注入各 Router。

### `api/session_manager.py`
Session 管理器。管理跨介面（HTML / Streamlit / Telegram / Unity）的對話狀態，支援自動過期清理。

---

## API 路由總覽

| 前綴 | Router 檔案 | 主要功能 |
|---|---|---|
| `/api/v1/health` | `health.py` | 健康狀態檢查 |
| `/api/v1/chat` | `chat_ws.py` | 同步/串流對話、WebSocket |
| `/api/v1/session` | `session.py` | Session CRUD、歷史記錄 |
| `/api/v1/memory` | `memory.py` | 記憶區塊 CRUD、搜尋、圖譜 |
| `/api/v1/profile` | `profile.py` | 使用者畫像管理 |
| `/api/v1/system` | `system.py` | 系統設定、人格、背景工具 |
| `/api/v1/prompts` | `prompts.py` | Prompt 範本即時編輯 |
| `/api/v1/logs` | `logs.py` | 系統日誌查詢 |
| `/api/v1/character` | `character.py` | 角色設定生成 |

詳細 API 規格請參閱 [API_使用說明書.md](API_使用說明書.md)。

---

## 前端介面

### Dashboard (`/static/dashboard.html`)
主要操作介面，透過 `http://localhost:8088/static/dashboard.html` 存取（需 FastAPI 運行）。
- **左側**：聊天對話區（SSE 串流、工具狀態、Debug 面板）
- **右側 Tab 1 — Log/DB**：內嵌 `log_viewer.html` 與 `db_viewer.html`
- **右側 Tab 2 — Routing**：LLM 任務路由即時設定
- **右側 Tab 3 — Prompts**：Prompt 範本即時編輯與重置

### Streamlit UI (`app.py`, Port 8501)
輔助管理後台，功能與 Dashboard 部分重疊，適合需要 Python 互動元件的情境。

---

## 資料儲存

| 檔案 | 內容 |
|---|---|
| `memory_db_<model>.db` | 每模型獨立的記憶區塊（向量、重要性、時間戳）與核心認知 |
| `conversation.db` | 對話 Session 與訊息歷史 |
| `user_prefs.json` | 執行期設定：API Keys、LLM 路由設定、各種閾值 |
| `ai_personality.md` | AI 人格演進檔案，由 PersonalityEngine 自動更新 |
| `system_prompt.txt` | AI 系統提示詞範本 |
| `prompts_default.json` | 所有任務的預設 Prompt 範本庫 |

### 關鍵閾值（`user_prefs.json`）

| 參數 | 說明 |
|---|---|
| `memory_threshold` | 向量相似度軟閾值（記憶召回） |
| `memory_hard_base` | 向量相似度硬閾值 |
| `shift_threshold` | 話題偏移偵測敏感度 |
| `cluster_threshold` | 記憶整合聚類閾值 |
| `reflection_threshold` | 觸發人格反思所需的觀察次數 |

---

## LLM 路由系統

每種任務類型可獨立設定 Provider 與 Model，透過 Dashboard Routing 頁或直接編輯 `user_prefs.json` 的 `routing_config` 欄位：

| 任務鍵 | 說明 | 建議 |
|---|---|---|
| `chat` | 即時對話（帶影子標籤） | 高參數量模型 |
| `expand` | User 意圖擴充 | 輕量快速模型 |
| `pipeline` | 一體化記憶管線 | 任意 |
| `compress` | 對話壓縮（編年史化） | 任意 |
| `distill` | 核心認知提煉 | 高品質模型 |
| `ep_fuse` | 情境概覽縫合 | 任意 |
| `profile` | 使用者畫像更新 | 任意 |
| `ai_observe` | AI 自我觀察提取 | 輕量快速模型 |
| `ai_reflect` | AI 人格反思 | 高品質模型 |
| `background_gather` | 背景話題摘要 | 輕量模型 |
| `character_gen` | 角色設定生成 | 高品質模型 |
| `router` | 意圖路由預處理 | 輕量快速模型 |

---

## 服務端口

| 服務 | 端口 | 說明 |
|---|---|---|
| FastAPI 後端 | **8088** | 主要 API 伺服器 |
| Streamlit UI | **8501** | 輔助管理後台 |
| Telegram Bot | — | 由 FastAPI lifespan 管理，無獨立端口 |
